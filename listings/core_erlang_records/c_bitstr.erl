-record(c_bitstr, {anno=[], 
                   val,     % val :: Tree,
                   size,    % size :: Tree,
                   unit,    % unit :: Tree,
                   type,    % type :: Tree,
                   flags}). % flags :: Tree
